# 会话进度日志{{VERSION_SUFFIX}}

> **协作规则（重要）：**
> - 本文件用 `merge=union` 合并：双方新增行都会保留、永不报冲突。
> - 因此**只能追加，不能改写**：只在自己 `## @<git user.name>` 节点下**往下追加**新条目。
> - **不要**编辑别人的节点；**不要**回头改自己已写的历史行（改写会被 union 复制成重复行）。
> - 你的 author 来自 `git config user.name`。没有自己的节点就新建一个。

---

## @your-git-username

### [YYYY-MM-DD HH:MM] 功能 id - 功能名

- 当前功能：[id] - [功能名]
- 进展：[这次做了什么]
- 验证：`[验证命令]` → [通过 / 失败原因]
- 下一步：[下一个动作]
- 改动文件：`path/to/file` — [简述]
