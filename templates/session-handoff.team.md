# 会话交接{{VERSION_SUFFIX}}

> **协作规则（重要）：**
> - 本文件用 `merge=union` 合并：双方新增行都会保留、永不报冲突。
> - 因此**只能追加，不能改写**：每次交接在自己 `## @<git user.name>` 节点下**新增一段**带时间戳的交接，不要覆盖上一段。
> - 想看最新状态就看自己节点最下面那段。
> - 你的 author 来自 `git config user.name`。

---

## @your-git-username

### [YYYY-MM-DD HH:MM] 交接

- 当前目标：[id] - [目标]
- 当前状态：[进行到哪]
- 分支 / 提交：[branch / commit]
- 验证证据：`[验证命令]` → [结果]
- 下一步明确动作：[具体动作]
- 阻塞 / 风险：[如有]
